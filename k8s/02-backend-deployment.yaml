apiVersion: apps/v1
kind: Deployment
metadata:
  name: cogni-backend
  namespace: cogni-dispatch
  labels:
    app: cogni-backend
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cogni-backend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: cogni-backend
        tier: backend
    spec:
      containers:
        - name: cogni-backend
<<<<<<< HEAD
          image: acrcogniutrawi.azurecr.io/cogni-backend:v1
=======
          # Replace acrcognidispatch with your ACR name
          image: acrcogni74p5p7.azurecr.io/cogni-backend:latest
>>>>>>> 095b5f2f866cdf641521791ef6311daf2a58a9ec
          imagePullPolicy: Always
          ports:
            - containerPort: 5000
              name: http
              protocol: TCP
          # Load all env vars from ConfigMap
          envFrom:
            - configMapRef:
                name: cogni-config
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          # Health probe — uses the /api/health endpoint
          livenessProbe:
            httpGet:
              path: /api/health
              port: 5000
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/health
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
