name: Build and Deploy Vendor Service

on:
  push:
    branches:
      - main
    paths:
      - 'services/vendor-service/**'
      - 'services/shared/**'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'services/vendor-service/package-lock.json'

      - name: Build dependencies
        run: |
          cd services/shared && npm install
          cd ../vendor-service && npm install
        
      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'cognidispatch-vendor'
          publish-profile: ${{ secrets.AZURE_WEBAPP_VENDOR_PUBLISH_PROFILE }}
          package: './services'
