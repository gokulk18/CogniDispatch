name: Build and Deploy Admin Service

on:
  push:
    branches:
      - main
    paths:
      - 'services/admin-service/**'
      - 'services/shared/**'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'services/admin-service/package-lock.json'

      - name: Build dependencies
        run: |
          cd services/shared && npm install
          cd ../admin-service && npm install
        
      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'cognidispatch-admin'
          publish-profile: ${{ secrets.AZURE_WEBAPP_ADMIN_PUBLISH_PROFILE }}
          package: './services'
