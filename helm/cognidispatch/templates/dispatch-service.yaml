{{- if .Values.dispatchService.enabled -}}
{{- if .Values.keyvault.enabled -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: dispatch-service-sa
  namespace: {{ .Values.global.namespace | default .Release.Namespace }}
  annotations:
    azure.workload.identity/client-id: {{ .Values.keyvault.clientId | quote }}
---
{{- end -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dispatch-service
  namespace: {{ .Values.global.namespace | default .Release.Namespace }}
  labels:
    app: dispatch-service
    tier: backend
    {{- if and .Values.keyvault.enabled .Values.keyvault.workloadIdentityEnabled }}
    azure.workload.identity/use: "true"
    {{- end }}
spec:
  replicas: {{ .Values.dispatchService.replicas }}
  selector:
    matchLabels:
      app: dispatch-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: dispatch-service
        tier: backend
        {{- if and .Values.keyvault.enabled .Values.keyvault.workloadIdentityEnabled }}
        azure.workload.identity/use: "true"
        {{- end }}
    spec:
      {{- if .Values.keyvault.enabled }}
      serviceAccountName: dispatch-service-sa
      {{- end }}
      containers:
        - name: dispatch-service
          image: "{{ .Values.global.imageRegistry }}/{{ .Values.dispatchService.image }}:{{ .Values.global.imageTag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          workingDir: /app/dispatch-service
          ports:
            - containerPort: 5005
              name: http
              protocol: TCP
          env:
            - name: PORT
              value: "5005"
            {{- if .Values.keyvault.enabled }}
            - name: MONGODB_URI_FILE
              value: "/mnt/secrets/MONGODB-URI"
            {{- end }}
          envFrom:
            - configMapRef:
                name: cogni-config
          resources:
            {{- toYaml .Values.dispatchService.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /api/dispatches/health
              port: 5005
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/dispatches/health
              port: 5005
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          {{- if .Values.keyvault.enabled }}
          volumeMounts:
            - name: secrets-store-inline
              mountPath: "/mnt/secrets"
              readOnly: true
          {{- end }}
      {{- if .Values.keyvault.enabled }}
      volumes:
        - name: secrets-store-inline
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: "cogni-secrets-provider"
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: dispatch-service
  namespace: {{ .Values.global.namespace | default .Release.Namespace }}
  labels:
    app: dispatch-service
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: dispatch-service
  ports:
    - name: http
      port: 5005
      targetPort: 5005
      protocol: TCP
{{- end -}}
