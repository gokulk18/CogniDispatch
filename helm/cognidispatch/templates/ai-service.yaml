{{- if .Values.aiService.enabled -}}
{{- if .Values.keyvault.enabled -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-service-sa
  namespace: {{ .Values.global.namespace | default .Release.Namespace }}
  annotations:
    azure.workload.identity/client-id: {{ .Values.keyvault.clientId | quote }}
---
{{- end -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-service
  namespace: {{ .Values.global.namespace | default .Release.Namespace }}
  labels:
    app: ai-service
    tier: backend
    {{- if and .Values.keyvault.enabled .Values.keyvault.workloadIdentityEnabled }}
    azure.workload.identity/use: "true"
    {{- end }}
spec:
  replicas: {{ .Values.aiService.replicas }}
  selector:
    matchLabels:
      app: ai-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: ai-service
        tier: backend
        {{- if and .Values.keyvault.enabled .Values.keyvault.workloadIdentityEnabled }}
        azure.workload.identity/use: "true"
        {{- end }}
    spec:
      {{- if .Values.keyvault.enabled }}
      serviceAccountName: ai-service-sa
      {{- end }}
      containers:
        - name: ai-service
          image: "{{ .Values.global.imageRegistry }}/{{ .Values.aiService.image }}:{{ .Values.aiService.imageTag | default .Values.global.imageTag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          workingDir: /app/ai-service
          ports:
            - containerPort: 5003
              name: http
              protocol: TCP
          env:
            - name: PORT
              value: "5003"
            {{- if .Values.keyvault.enabled }}
            - name: MONGODB_URI_FILE
              value: "/mnt/secrets/MONGODB-URI"
            - name: AZURE_OPENAI_KEY_FILE
              value: "/mnt/secrets/AZURE-OPENAI-KEY"
            - name: AZURE_SPEECH_KEY_FILE
              value: "/mnt/secrets/AZURE-SPEECH-KEY"
            {{- end }}
          envFrom:
            - configMapRef:
                name: cogni-config
          resources:
            {{- toYaml .Values.aiService.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /api/ai/health
              port: 5003
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/ai/health
              port: 5003
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          {{- if .Values.keyvault.enabled }}
          volumeMounts:
            - name: secrets-store-inline
              mountPath: "/mnt/secrets"
              readOnly: true
          {{- end }}
      {{- if .Values.keyvault.enabled }}
      volumes:
        - name: secrets-store-inline
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: "cogni-secrets-provider"
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: {{ .Values.global.namespace | default .Release.Namespace }}
  labels:
    app: ai-service
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: ai-service
  ports:
    - name: http
      port: 5003
      targetPort: 5003
      protocol: TCP
{{- end -}}
